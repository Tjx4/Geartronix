<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="co.za.geartronix">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Activities -->
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".activities.GalleryActivity"
            android:parentActivityName=".activities.DashBoardActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.DashBoardActivity"/>
        </activity>
        <activity android:name=".activities.CollToolbarActivity"/>
        <activity
            android:name=".activities.DashBoardActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".activities.ServicesActivity"
            android:parentActivityName=".activities.DashBoardActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.DashBoardActivity"/>
        </activity>
        <activity android:name=".activities.ProfileActivity">
        </activity>
        <activity android:name=".activities.FindUsActivity">
        </activity>
        <activity
            android:name=".activities.MessagesActivity"
            android:parentActivityName=".activities.DashBoardActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.DashBoardActivity"/>
        </activity>

        <!-- Services -->


        <!-- Providers -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="list"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths"/>
        </provider>

        <activity android:name=".activities.DiagnosticsActivity">
        </activity>
        <activity android:name=".activities.ServicesListActivity">
        </activity>
        <activity
            android:name=".activities.RegistrationActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".activities.SettingsActivty">
        </activity>
        <activity android:name=".activities.ForgotPasswordActivity">
        </activity>
        <activity android:name=".activities.AskActivity">
        </activity>
        <activity android:name=".activities.ReferralActivity">
        </activity>
    </application>

</manifest>